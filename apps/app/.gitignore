# TanStack Start / Nitro build output and caches
.output/
.tanstack/
.nitro/
dist/

# Netlify local state/build cache
.netlify/

# scratch output of `pnpm auth:generate` — hand-merge into src/server/db/schema.ts
# (house style: one schema.ts) instead of committing this file directly.
src/server/auth/auth-schema.generated.ts

# generated by @tanstack/react-router's vite plugin at dev/build time; regenerated
# on every build, but committed so a fresh checkout can run `tsc --noEmit` without
# first starting a dev server. Do NOT hand-edit.
# (kept in git — see src/routeTree.gen.ts)
